{
  "name": "kailift-wx",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check-project.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
